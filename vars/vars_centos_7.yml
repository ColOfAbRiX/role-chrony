---

chrony_pre_pkgs:
 - ca-certificates
 - epel-release
 - libselinux-python

chrony_pkgs:
 - chrony

chrony_path_info:
  conf_path: /etc/chrony.conf
  chronyc_path: /usr/bin/chronyc

chrony_service_info:
  name: chronyd
